# 1. Install dependencies
pip install -r requirements.txt

# 2. Train the model (creates models/ directory automatically)
python train_model.py

# 3. Run the web app
python app.py

# 4. Open browser at: http://127.0.0.1:5000